import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.psnti.routecalc',
  appName: 'PSNTI RouteCalc',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;